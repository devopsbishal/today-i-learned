# Today I Learned

A personal learning journal where I document what I learn each day. Writing things down helps me understand concepts better and gives me a reference to look back on when I need a refresher.

---

## 📚 What's This?

This is my collection of bite-sized learnings - concepts, insights, and "aha moments" I pick up along the way. Each entry is written in my own words, often with real-world analogies that help me remember.

## 📁 Structure

```
today-i-learned/
├── 2025/
│   ├── november/
│   │   └── 2025-11-30-aws-vpc.md
│   └── december/
├── 2026/
│   └── ...
└── Readme.md
```

Organized by: `year/month/YYYY-MM-DD-topic.md`

## 📝 Recent Learnings

| Date | Topic | Description |
|------|-------|-------------|
| 2026-04-07 | [AWS API Gateway Deep Dive](./2026/april/aws/2026-04-07-api-gateway-deep-dive.md) | API Gateway as managed API layer deep dive (grand hotel concierge analogy -- full-service desk as REST API, express desk as HTTP API, telephone switchboard as WebSocket API, hotel floors as stages, token bucket as ticket dispenser, filing cabinet as cache, bouncer as Lambda authorizer, badge scanner as Cognito authorizer, membership program as usage plans), three API types comparison (REST API $3.50/M with full features caching/WAF/usage-plans/VTL/canary/private-endpoints/request-validation/SDK-generation vs HTTP API $1.00/M 71% cheaper with native JWT authorizer and ~10ms p99 latency but no caching/WAF/usage-plans/VTL/canary vs WebSocket API $1.00/M messages + $0.25/M connection-minutes with $connect/$disconnect/$default routes and @connections callback API for server push), four-option decision matrix Function-URLs -> HTTP-API -> REST-API -> ALB ordered by features and cost, stages as named deployment references with independent caching/throttling/logging/variables per stage, stage variables in integration URIs routing to different Lambda aliases per stage (connecting to Lambda versions/aliases from Apr 6), throttling token bucket algorithm with four-tier priority hierarchy (usage-plan per-client per-method > method-level > stage-level > account-level 10K RPS 5K burst default soft limit), caching per-stage REST-API-only (0.5-237 GB, 300s default TTL max 3600s, cache keys from parameters, invalidation via Cache-Control max-age=0 requiring execute-api:InvalidateCache IAM permission, changing cache size deletes all data, ~4 min create/delete), Lambda authorizers TOKEN type (bearer token from header cached by token value) vs REQUEST type (full request context cached by configured identity sources, recommended for new implementations) with authorization caching gotchas (misconfigured identity sources can leak policies across users), Cognito user pool authorizer (JWT validation without Lambda invocation, authentication only not authorization), HTTP API native JWT authorizer (any OIDC provider Cognito/Auth0/Okta/Firebase without Lambda), IAM authorization for service-to-service with SigV4, usage plans and API keys (tiered per-client throttling and quotas, API keys are identification NOT authentication, Silver/Gold tier pattern), five integration types (AWS_PROXY Lambda proxy as 90% default passing full request with stringified body response requirement, AWS non-proxy with VTL mapping templates for direct AWS service integrations DynamoDB/SQS/Step-Functions without Lambda, HTTP_PROXY passthrough, HTTP with transformation, MOCK for CORS preflight and prototyping), VTL mapping templates for request/response transformation in non-proxy integrations, canary deployments as stage-level traffic splitting (configurable percentage with separate CloudWatch logs and stage variable overrides, complementary to Lambda weighted alias routing -- canary tests API config while alias tests function code), three endpoint types (Regional recommended with own CloudFront for full control, Edge-Optimized with AWS-managed CloudFront you cannot configure, Private accessible only via Interface VPC Endpoints), private APIs with resource policies restricting to specific VPCs/VPC-endpoints, resource policy + IAM authorization AND logic (both must Allow, implicit deny from resource policy + IAM Allow = final Deny), WAF integration REST-API-only (web ACL on stage with managed rules/rate-based/IP/geo-blocking, evaluates before API Gateway processing), mutual TLS requiring custom domain name with S3 truststore (REST and HTTP API), CORS configuration (REST API manual OPTIONS mock integration vs HTTP API one-click), request validation REST-API-only (JSON Schema models reject malformed requests before Lambda invocation saving cost), gateway response customization (THROTTLED/UNAUTHORIZED/ACCESS_DENIED/INTEGRATION_FAILURE with custom headers/body/status), observability (access logging structured JSON to CloudWatch always-on vs execution logging verbose pipeline trace ERROR-default INFO-for-debugging never permanently, key metrics Count/4XX/5XX/Latency/IntegrationLatency/CacheHitCount with Latency-minus-IntegrationLatency as gateway overhead), binary media types base64 encoding, SDK generation REST-API-only, VPC Links (REST API to NLB only, HTTP API to ALB/NLB/Cloud Map), complete limits reference (29s REST/30s HTTP timeout, 10MB payload, 10K RPS account default, 300 resources per API, 10 stages), pricing (REST $3.50/M + cache hourly, HTTP $1.00/M, WebSocket $1.00/M msgs + $0.25/M conn-min, 12-month free tier NOT perpetual unlike Lambda), production Terraform with REST API Regional endpoint + Lambda proxy integration with stage variable routing + TOKEN Lambda authorizer with 300s cache + request validation JSON Schema model + stage-level 0.5GB cache with TTL and encrypted with authorization-required invalidation + method-level throttling 500 RPS + stage-level default 1000 RPS + canary deployment 10% with stage variable override + usage plan Silver tier 1000/day 10 RPS with per-method POST override + API key + WAF web ACL with Common/SQLi managed rules and rate limiting + resource policy IP allowlist + custom gateway responses THROTTLED/UNAUTHORIZED + CloudWatch access logging JSON format + execution logging ERROR level + X-Ray tracing + CloudWatch alarms 5XX and p99 latency + CORS OPTIONS mock integration + HTTP API with native JWT Cognito authorizer and OAuth scopes for comparison, 15 critical gotchas including API-keys-not-authentication, REST-29s-not-30s-timeout, proxy-body-must-be-string-502, cache-size-change-deletes-data, cache-invalidation-without-auth-stampede-vulnerability, authorizer-caching-leaks-policies, resource-policy-plus-IAM-AND-logic, WAF-REST-only, edge-optimized-hidden-uncontrollable-CloudFront, HTTP-API-no-validation-no-VTL-no-mock, WebSocket-disconnect-best-effort, api-gateway-account-global-singleton, canary-vs-alias-different-levels, REST-free-tier-12-months-not-perpetual, mTLS-requires-custom-domain |
| 2026-04-06 | [AWS Lambda Deep Dive](./2026/april/aws/2026-04-06-lambda-deep-dive.md) | Lambda execution model and serverless compute deep dive (on-demand kitchen analogy -- cooking stations as Firecracker microVMs, station lifecycle as Init/Invoke/Shutdown phases, preheating as cold start mechanics, capacity planning as concurrency controls, recipe book as versions/aliases), Firecracker microVM internals (<125ms boot, KVM hardware-level isolation, <5 MiB overhead per VM), execution environment lifecycle (Init three sub-phases Extension/Runtime/Function init with 10s on-demand vs 15min provisioned timeout, environment freeze/thaw between invocations, /tmp persistence within same environment, static initialization outside handler as #1 optimization), cold start deep dive (four triggers: new deployment/scaling/idle timeout/config change, duration factors by runtime Python ~200ms vs Java ~3s+, package size impact, memory=CPU proportional scaling with 1769 MB as 1-vCPU sweet spot, VPC Hyperplane eliminating historic 10-30s penalty to <1ms), cold start mitigation decision tree (SnapStart for cost-sensitive sub-second tolerance vs Provisioned Concurrency for guaranteed double-digit-ms, code-level optimizations always applicable), concurrency model (formula requests/s x duration, three tiers: unreserved shared pool with 100 always-kept-back, reserved as free capacity guarantee AND cap, provisioned as paid pre-warming -- independent controls that combine), burst concurrency 500-3000 by region then +500/min sustained, throttling behavior by invocation model (sync 429 to caller vs async 2 retries then destination vs ESM source-dependent), Provisioned Concurrency Auto Scaling (target tracking at 70% utilization + scheduled pre-warming for predictable rushes), Lambda Layers (shared ZIP to /opt, 5 layers max, 250 MB total unzipped, immutable versions, cross-account sharing), three invocation models with distinct error contracts (synchronous caller-responsible zero retry, asynchronous Lambda-managed 0-2 retries with Destinations superseding DLQs for richer metadata including full invocation record with success+failure routing to SQS/SNS/Lambda/EventBridge, event source mapping source-dependent), Destinations vs DLQ comparison (Destinations support success+failure routing with full metadata vs DLQ failure-only with event payload only), event source mapping deep dive (Lambda-managed pollers not push model -- stream sources Kinesis/DDB Streams with in-order per-shard processing up to 10K batch size with parallelization factor 1-10 and BisectBatchOnFunctionError recursively halving to isolate poison records + MaximumRetryAttempts + on-failure destination as production pattern, queue sources SQS with automatic scaling and DLQ on the SQS queue not Lambda via redrive policy, ReportBatchItemFailures for partial success, event filtering at service level with non-matching SQS messages permanently deleted, tumbling windows for stream aggregation), critical SQS DLQ distinction (configured on SQS queue redrive policy not Lambda function -- most confused interview topic), VPC integration via Hyperplane ENIs (shared per subnet+SG combination not per environment, requires private subnet with NAT Gateway or VPC endpoints for internet/AWS API access, AWSLambdaVPCAccessExecutionRole managed policy), versions as immutable code+config snapshots with sequential numbering and aliases as mutable named pointers enabling decoupled deployment, weighted alias routing for canary deployments (primary + one secondary version with 0.0-1.0 weight), CodeDeploy integration (Canary/Linear/AllAtOnce strategies with pre/post-traffic hooks and automatic CloudWatch Alarm rollback -- Lambda equivalent of ECS blue-green), SnapStart snapshot-based cold start optimization (Java 11+/Python 3.12+/.NET 8+, up to 90% cold start reduction, Firecracker memory+disk snapshot at publish time restored on cold start, runtime hooks beforeCheckpoint/afterRestore for refreshing DB connections/RNG/credentials, mutually exclusive with Provisioned Concurrency, no EFS/container images/512MB ephemeral limit), Lambda@Edge Lambda-service specifics (must author in us-east-1, published versions only, replicated to 15 regional edge caches -- CDN comparison in CloudFront Mar 13 doc), Lambda extensions internal in-process vs external sidecar for monitoring/observability with own lifecycle participation, recursive loop detection circuit breaker for Lambda-SQS-Lambda and Lambda-SNS-Lambda and Lambda-S3-Lambda after ~16 invocations, complete Lambda limits reference (15min timeout, 10GB memory, 250MB unzipped deployment, 10GB /tmp, 1000 default concurrent, 6MB sync payload, 256KB async payload), pricing model (requests $0.20/1M + duration $0.0000166667/GB-s + provisioned capacity charges, perpetual free tier 1M requests + 400K GB-s monthly -- not 12-month limited), production Terraform with Lambda function in VPC with layers + published version with prod alias + reserved concurrency 200 + provisioned concurrency 10 with Auto Scaling target tracking 70% + scheduled morning rush pre-warming + async destinations EventBridge success and SQS failure + SQS event source mapping with filtering and ReportBatchItemFailures and scaling_config maximum_concurrency + DynamoDB Streams event source mapping with bisect-on-error and parallelization factor and on-failure destination + CloudWatch alarms for errors/throttles/DLQ depth, ten critical gotchas including provisioned-does-not-set-reserved independence, SQS-DLQ-on-queue-not-Lambda, SnapStart-vs-provisioned mutual exclusion, VPC-internet-access-requires-NAT, weighted-alias-exactly-two-versions, Lambda@Edge-must-be-us-east-1, $LATEST-incompatible-with-aliases/SnapStart/Edge, SQS-visibility-timeout-6x-Lambda-timeout, filter-deletes-non-matching-SQS-messages |
| 2026-04-03 | [Storage DR & Data Replication](./2026/april/aws/2026-04-03-storage-dr-data-replication.md) | Storage-layer DR operational mechanics (museum conservation analogy -- traveling exhibitions as replication for infrastructure failure vs archival conservation as backup for logical errors), S3 Cross-Region Replication deep dive (what replicates vs what does NOT: existing objects require Batch Replication, SSE-C objects never replicate, Glacier/Deep Archive objects blocked, lifecycle actions do not replicate requiring independent policies on each bucket, replicas do not cascade A->B->C, bucket configs excluded), delete marker replication (OFF by default in V2 Filter configs vs ON by default in V1 Prefix configs, permanent version ID deletions NEVER replicate as intentional security protection against cross-region malicious deletion), S3 Replication Time Control (99.9% within 15 minutes SLA -- fixed non-configurable threshold, auto-enables CloudWatch metrics ReplicationLatency/BytesPending/OperationsPending/OperationsFailed, event notifications for missed/caught-up threshold, 1 Gbps default transfer quota), S3 Batch Replication (three scenarios: pre-rule existing objects/failed replications/newly added destinations, auto-generated manifest from inventory, unbounded RPO during backfill window), S3 versioning as recovery mechanism (accidental overwrite/delete/corruption/ransomware recovery patterns, version accumulation cost management with lifecycle noncurrent expiration + newer_noncurrent_versions), DynamoDB Global Tables MREC vs MRSC DR comparison (async sub-second vs synchronous RPO=0, any regions vs predefined sets US/EU/AP only, MRSC hard constraints: no LSI/no TTL/no transactions/no Streams/no export-import/cannot change after creation/cannot add replicas/source must be empty, witness architecture for cost savings), DynamoDB PITR (per-second granularity, January 2025 configurable 1-35 day retention, pricing same regardless of period, restore creates NEW table inheriting nothing -- no Streams/auto-scaling/IAM/alarms/tags/Global Table config, safety net 35-day system backup on table deletion, the ONLY corruption defense for Global Tables), DynamoDB Export to S3 (full/incremental zero-RCU reads from PITR stream, cross-account cross-region, DynamoDB JSON/Ion/CSV formats, GZIP/ZSTD compression, Import creates new table zero WCU), three-tier DynamoDB protection stack (Global Tables for instant Region failover + PITR for corruption rollback + Export to S3 cross-account cold backup), S3 defense-in-depth five layers (versioning foundation + CRR with RTC + AWS Backup with Vault Lock + Object Lock on destination + MFA Delete), replication-is-not-backup principle applied to both S3 and DynamoDB (replication copies corruption as fast as good data, every replication mechanism needs independent backup underneath), storage DR decision tree for S3 and DynamoDB, cost comparison tables for both services, production Terraform with multi-region KMS keys + S3 primary/DR buckets with versioning + independent lifecycle policies + CRR with RTC and delete marker replication + SSE-KMS with source selection criteria + CloudWatch replication lag and failure alarms + DynamoDB Global Table MREC with PITR on both replicas + Streams + TTL + GSI + cross-region S3 export bucket with tiered lifecycle |
| 2026-04-02 | [Database Disaster Recovery Patterns](./2026/april/aws/2026-04-02-database-disaster-recovery.md) | Database DR operational mechanics (emergency room triage analogy), RDS backup internals (daily snapshot + 5-minute transaction log archival to S3, PITR as snapshot restore + log replay, I/O suspension for single-AZ only, 0-35 day retention, restore-creates-new-instance constraint), manual snapshots vs automated backups (persist beyond retention, shareable cross-account, no automatic cleanup, deleted-with-instance gotcha), RDS cross-region automated backup replication (replicates snapshots AND transaction logs enabling cross-region PITR -- unlike AWS Backup which copies snapshots only, not Aurora, not Multi-AZ DB Cluster, max 20 per account), Aurora backup differences (continuous incremental to S3 with zero performance impact, no backup window, no I/O suspension), Aurora Backtrack (in-place rewind without new instance, MySQL only, 72-hour max, must enable at creation, entire cluster not individual tables), Aurora Global Database failover deep dive (managed planned switchover with RPO=0 via switchover-global-cluster pauses writes + drains replication + demotes primary + promotes secondary + preserves topology + global writer endpoint auto-updates, vs managed unplanned failover via failover-global-cluster --allow-data-loss promoting immediately + epoch-based write fencing preventing split-brain + automatic rds:unplanned-global-failover-* snapshot + old primary detached requiring manual cleanup), global writer endpoint eliminating post-failover endpoint reconfiguration as data-plane-like mechanism, failback procedure after unplanned failover (delete stale old primary + create new secondary + wait for replication + managed switchover back vs DynamoDB effortless failback), cross-region vs in-region failover as completely different mechanisms (shared storage promotion ~30s vs storage-level replication switchover ~1-2min), RDS cross-region read replica promotion (manual irreversible breaks replication, engine support differences, Aurora PostgreSQL has NO cross-region read replicas only Global Database), RDS Proxy failover acceleration (holds client connections during Multi-AZ failover reducing 60-120s disruption to under 10s by absorbing DNS propagation, connection pooling/multiplexing, IAM auth + Secrets Manager integration), connection pinning (session variables/prepared statements/temp tables prevent multiplexing and disrupt failover), RDS Proxy single-region-only gotcha (no cross-region benefit, deploy one per region), database DR decision framework mapping RPO/RTO to patterns (AWS Backup cross-region for RPO hours, RDS cross-region backup replication for RPO 5min with cross-region PITR, RDS cross-region read replica for RPO minutes, Aurora Global DB for RPO seconds, Aurora Global DB Active-Active for RPO near-zero), AWS Backup vs native replication decision (Backup for governance/corruption protection, native replication for tight RPO/RTO, use both), production Terraform with Aurora Global Database primary/secondary clusters + RDS Proxy in both regions + cross-region automated backup replication for legacy RDS instance |
| 2026-04-01 | [AWS Backup & Centralized Protection](./2026/april/aws/2026-04-01-aws-backup-centralized-protection.md) | AWS Backup as centralized backup orchestration layer across 20+ services (corporate records management department analogy -- not creating original documents but orchestrating when copies are made, where stored, how long kept, and whether the system works), backup plans (scheduling rules with cron expressions, start/completion windows, lifecycle transitions to cold storage ~75% savings, retention periods, cross-region copy actions), backup selections (tag-based resource assignment as scalable enterprise pattern, ARN-based explicit selection, wildcard), three-tier vault hierarchy (standard vault with access policies + KMS encryption, Vault Lock Governance mode privileged-admin-overridable, Vault Lock Compliance mode irreversible after 3-day cooling-off period even root/AWS Support cannot remove matching S3 Object Lock pattern, Logically Air-Gapped vault stored in AWS service-owned account surviving full account compromise shared via AWS RAM for restore), cross-region backup copies (copy_action within backup rules with independent lifecycle), cross-account backup (dedicated backup account pattern with restrictive SCPs as separate blast radius, vault access policies granting CopyIntoBackupVault), Organization backup policies (merging not overriding unlike SCPs, inheritance through OU hierarchy, delegated administrator up to 5 accounts, organization-level opt-in overrides member settings), feature availability matrix by resource type (cold storage only EBS/EFS/DynamoDB/VMware, continuous backup PITR only S3/RDS/Aurora/DynamoDB/SAP HANA/Timestream, air-gapped vault support expanding), restore testing (automated restore validation with recovery point selection, validation windows 1-168 hours for custom checks, automatic cleanup, monthly schedule recommendation, dedicated test account best practice), Backup Audit Manager (compliance frameworks with control templates evaluating backup coverage/encryption/frequency/vault lock/cross-region/cross-account, AWS Config prerequisite, daily S3 reports), AWS Backup vs service-native backups decision framework (cross-account copies/compliance auditing/organization policy enforcement/vault lock/restore testing require AWS Backup, orchestration is free pay only for storage and transfer same as service-native), cost considerations (per-GB-month storage varying by resource type, cold storage ~75% savings, cross-region transfer ~$0.02/GB, restore fees for DynamoDB/S3, orchestration free), multi-region KMS keys for cross-region encrypted backup restores, production Terraform example with Compliance-mode Vault Lock primary and DR vaults + multi-region KMS keys + daily/monthly backup plans with cross-region copy + tag-based selection + Audit Manager compliance framework with five controls + report plan + restore testing plan |
| 2026-03-31 | [Multi-Region Architecture Patterns](./2026/march/aws/2026-03-31-multi-region-architecture-patterns.md) | Multi-region architecture as system-level composition (multinational corporation analogy), data plane vs control plane as the #1 multi-region principle (Route53 control plane in us-east-1, health-check-based failover uses data plane, ARC routing controls as data-plane switches), static stability (pre-provisioned resources that operate without control-plane calls), Active-Active vs Active-Passive decision framework (write conflict complexity as primary driver, latency/cost/compliance/operational maturity factors), multi-region data patterns (Read Local Write Global with Aurora Global DB write forwarding, Read Local Write Local with DynamoDB Global Tables MREC last-writer-wins, Read Local Write Local Strong with DynamoDB MRSC 3-region quorum, Read Local Write Partitioned with S3 bidirectional CRR, cache tier with ElastiCache Global Datastore), composing data patterns per data type in same application (relational for catalog + NoSQL for sessions + object storage for uploads), multi-region routing stack (CloudFront origin failover groups + Route53 latency/failover routing + Global Accelerator network-layer failover + ARC routing controls), Route53 vs Global Accelerator failover comparison (DNS caching problem vs instant network-layer reroute), shared fate and dependency analysis (us-east-1 control plane dependencies, third-party services, cross-region synchronous calls anti-pattern, monitoring in failed region), CAP theorem applied to AWS services (DynamoDB MREC as AP, DynamoDB MRSC as CP with 3-region quorum, Aurora Global DB as CP-writes/AP-reads), PACELC extension (latency vs consistency even without partitions), region selection priority order (Compliance > Latency > Cost > Service Availability), operational patterns (sequential deployment as #1 outage prevention, monitor FROM outside the region, failover automation maturity ladder, quarterly game days with FIS), ten anti-patterns (control-plane failover, untested DR, partial failover creating split-brain, cross-region sync calls, monitoring in failed region, simultaneous multi-region deployment), production Active-Active architecture diagram with Aurora Global DB + DynamoDB Global Tables + ElastiCache Global Datastore + S3 bidirectional CRR + Global Accelerator + Route53 latency routing + ARC routing controls, complete Terraform examples (Global Accelerator with dual endpoint groups, Route53 latency routing with health checks, Aurora Global Database with write forwarding, DynamoDB Global Table with PITR, S3 bidirectional CRR), cost framework (Active-Passive +25-50% vs Active-Active +80-120%, cost optimization with Graviton/Serverless v2/on-demand), critical exam gotchas (Route53 control plane in us-east-1, data-plane-only failover, DynamoDB MRSC as only strongly consistent cross-region option, sequential deployment prevents #1 outage cause) |
| 2026-03-30 | [AWS Disaster Recovery Strategies](./2026/march/aws/2026-03-30-disaster-recovery-strategies.md) | The four DR strategies on the cost-recovery spectrum (Backup & Restore with RPO hours/RTO hours-to-days at lowest cost, Pilot Light with RPO minutes/RTO tens of minutes keeping data tier live but compute off, Warm Standby with RPO seconds/RTO minutes running scaled-down full stack, Multi-Site Active-Active with RPO/RTO near-zero at highest cost), RPO/RTO as business decisions mapped to technical strategy selection, hospital emergency preparedness analogy (rural clinic fireproof safe to two full hospitals in different cities), Pilot Light vs Warm Standby critical distinction (compute running vs not -- the most tested exam question), Active-Active three write strategies (Write Global with Aurora Global Database single-writer + write forwarding, Write Local with DynamoDB Global Tables active-active MREC last-writer-wins, Write Partitioned with S3 bidirectional CRR), replication is not backup principle (data corruption replicates instantly across Regions, PITR/versioning required in every strategy), Multi-AZ is HA not DR distinction (AZ-level vs Region-level protection), when to promote read replica vs failover to standby (Aurora Global DB managed switchover zero-data-loss vs unplanned failover RPO ~1s, RDS cross-region replica manual promote with replication-lag RPO, DynamoDB Global Tables no promotion needed), AWS Elastic Disaster Recovery (DRS) achieving Warm Standby RPO/RTO at Pilot Light cost via continuous block replication with automated compute provisioning, AWS services mapped to DR roles (AWS Backup centralized cross-region, S3 CRR, Aurora Global Database, DynamoDB Global Tables, ElastiCache Global Datastore, EFS replication, Route53 failover routing, Global Accelerator, CloudFormation StackSets, Application Recovery Controller ARC), failover decision tree from AZ failure to Region failure, critical gotchas (Auto Scaling is control-plane operation during DR, service quotas must match in DR Region, failback harder than failover), cost estimation framework (5-10% for Backup & Restore to 100% per additional Region for Active-Active), production Warm Standby Terraform example with Aurora Global Database primary/secondary clusters and Route53 failover routing and AWS Backup cross-region copy rules |
| 2026-03-27 | [ElastiCache & Caching Strategies](./2026/march/aws/2026-03-27-elasticache-caching-strategies.md) | ElastiCache fundamentals (managed in-memory caching layer with Redis/Valkey and Memcached engines, deli counter analogy for cache-in-front-of-database pattern), Redis vs Memcached (Swiss Army knife vs single-blade knife, Redis data structures/persistence/replication/clustering vs Memcached multi-threaded simplicity, Valkey as BSD-licensed Redis fork 20-33% cheaper), caching strategies (lazy loading/cache-aside as universal baseline with cache-miss three-trip penalty, write-through for always-fresh hot data with wasted-memory trade-off, write-behind/write-back for lowest write latency with data-loss risk, TTL as universal safety net, combined strategy stack matching DAX's native behavior), cache invalidation (TTL-based vs explicit vs event-driven, thundering herd/dog-piling with four mitigations: mutex lock/pre-warming/staggered TTL jitter/background refresh), Cluster Mode Disabled vs Enabled (single shard with primary + 5 replicas like Aurora single writer vs up to 500 shards with 16384 hash slots for horizontal write scaling like DynamoDB partitions, hash tag requirement for multi-key operations, online resharding), ElastiCache Serverless (ECPU-based pricing model paralleling Aurora Serverless v2 and DynamoDB on-demand, mandatory encryption, single endpoint, auto-scaling), Global Datastore (single-writer + up to 2 read-only secondary regions with sub-second lag, requires Cluster Mode Enabled and node-based only, comparison with Aurora Global Database single-writer model and DynamoDB Global Tables active-active model), Redis data structures (sorted sets for leaderboards/rate-limiters, streams with consumer groups for event processing, pub/sub fire-and-forget messaging, hashes for field-level object access, geospatial indexes), ElastiCache vs DAX decision framework (DAX for DynamoDB-only zero-code-change acceleration, ElastiCache for everything else including cross-service caching/aggregated results/rich data structures), security (VPC-only deployment, encryption at rest + in transit mandatory for Serverless/optional-but-creation-time-only for node-based, AUTH token/IAM authentication/RBAC with access strings), eviction policies (eight Redis policies with allkeys-lru recommended default, volatile-lru gotcha where keys without TTL are never evicted), monitoring (EngineCPUUtilization for single-threaded Redis bottleneck detection, CacheHitRate, evictions trend), data tiering (r6gd SSD+memory nodes for 5x data at 60% lower cost), cost optimization (Reserved Nodes up to 55% savings, Valkey default, right-sizing, Serverless for variable workloads), production e-commerce architecture with ElastiCache Cluster Mode Enabled fronting both Aurora and DynamoDB plus Global Datastore for multi-region session reads, Terraform examples for node-based and Serverless configurations |
| 2026-03-26 | [DynamoDB Deep Dive](./2026/march/aws/2026-03-26-dynamodb-deep-dive.md) | DynamoDB fundamentals (schemaless key-value/document model, paradigm shift from relational design-schema-first to design-access-patterns-first), primary keys (simple partition key with hash-based distribution, composite partition key + sort key enabling range queries, item collections), partition key design (hot partition problem with 3K RCU/1K WCU per-partition ceiling, high-cardinality principle, write-sharding pattern with random suffix, adaptive capacity as safety net not replacement), secondary indexes (GSI with independent keys/throughput/no size limit/eventual consistency only/created anytime vs LSI with shared partition key/shared throughput/10 GB item collection limit/strong consistency support/creation-time-only, index projections KEYS_ONLY/INCLUDE/ALL with multiplicative write cost, sparse indexes, overloaded GSI pattern for single-table design, practical rule default to GSI), capacity modes (on-demand pay-per-request with auto-scaling to 2x previous peak vs provisioned with auto-scaling and 5-10 min reaction delay, RCU/WCU unit definitions with consistency multipliers, peak-to-average ratio decision heuristic, reserved capacity up to 75% savings, switching constraints, comparison to Aurora Serverless v2 vs provisioned), DAX (fully managed VPC-based in-memory cache, API-compatible drop-in SDK replacement, microsecond reads, two caches item cache + query cache with separate TTLs, write-through for item cache but query cache relies on TTL, eventually consistent reads only, DAX vs ElastiCache decision), DynamoDB Streams (time-ordered deduplicated CDC with 24h retention, four StreamViewType options KEYS_ONLY/NEW_IMAGE/OLD_IMAGE/NEW_AND_OLD_IMAGES set once, per-item ordering guarantee, Lambda trigger integration with free polling, Kinesis Data Streams alternative for 365-day retention, comparison to RDS/Aurora limited CDC), Global Tables (multi-region active-active writes vs Aurora Global Database single-writer, MREC last-writer-wins default vs MRSC strong consistency across 3 regions, 99.999% SLA, per-region DAX caches, per-region transaction isolation, per-region streams), single-table design (pre-joining heterogeneous entity types via composite keys PK=CUSTOMER#id SK=ORDER#date, compensating for lack of JOINs, overloaded GSI pattern, single-table vs multi-table decision framework), transactions (ACID across up to 100 items at 2x cost, single region only), TTL (automatic zero-cost deletion with stream records, cross-region replication), PITR (35-day continuous backup, restores to new table), encryption (always encrypted unlike RDS optional, three key options, changeable on existing tables), DynamoDB vs RDS/Aurora decision framework (JOINs/ad-hoc queries/complex transactions favor RDS, known access patterns/serverless/global active-active/microsecond latency favor DynamoDB), production e-commerce architecture with DynamoDB for OLTP + Aurora for OLAP + Streams-to-Firehose-to-S3-to-Athena analytics bridge |
| 2026-03-25 | [RDS & Aurora Deep Dive](./2026/march/aws/2026-03-25-rds-aurora-deep-dive.md) | RDS fundamentals (six engines MySQL/PostgreSQL/MariaDB/Oracle/SQL Server/Db2, instance classes db.t/m/r/x families with Graviton, EBS storage types gp3/io2/magnetic), RDS Multi-AZ classic (synchronous block-level replication to single standby that serves zero traffic, DNS CNAME failover in 60-120s, all six engines), RDS Multi-AZ DB Cluster (one writer + two readable standbys across 3 AZs, semisynchronous transaction log shipping, reader endpoint, failover under 35s, MySQL/PostgreSQL only), RDS Read Replicas (asynchronous engine-level replication, up to 5 per source, cross-region capable, promotable to standalone, daisy-chaining, combinable with Multi-AZ), Aurora architecture (compute-storage separation, shared distributed storage volume auto-scaling 10 GiB-128 TiB, 6 copies across 3 AZs in 10 GB segments, 4/6 write quorum tolerates AZ loss, 3/6 read quorum tolerates AZ+1 loss, quorum overlap guarantee 4+3>6, sub-10-second segment repair on 10 Gbps network, peer-to-peer gossip self-healing), Aurora replicas (up to 15, zero additional storage cost, shared volume, single-digit ms lag, automatic failover promotion vs RDS manual promotion), Aurora endpoints (cluster/reader/custom/instance), Aurora I/O-Optimized vs Standard (25% I/O cost threshold decision rule, 2.25x storage cost but zero per-I/O charges, introduced 2023 to eliminate surprise billing), Aurora Serverless v2 (ACU scaling 0.5-256 in 0.5 increments, 1 ACU = 2 GiB RAM, independent per-instance scaling, promotion tiers 0-1 scale with writer for failover readiness vs tiers 2-15 scale independently, mixed clusters with provisioned writer + Serverless readers), Aurora Global Database (storage-level cross-region replication with sub-second lag, up to 5 secondary regions with 16 readers each, managed planned switchover with zero data loss vs unplanned failover with RPO in seconds/RTO in minutes, write forwarding from secondary to primary), Aurora Cloning (copy-on-write protocol, minutes regardless of DB size, zero initial storage, 15-clone limit, same region only, vs snapshot restore full-copy comparison), RDS Proxy (connection pooling solving Lambda connection storms, IAM + Secrets Manager auth, failover acceleration, pinning detection), RDS automated backups vs manual snapshots (PITR within 0-35 day retention vs indefinite point-in-time, Aurora continuous backup to S3), Aurora vs RDS decision framework (engine availability as first fork, cost/scale/replication/cloning/serverless evaluation criteria) |
| 2026-03-24 | [Block & File Storage -- EBS, EFS, FSx](./2026/march/aws/2026-03-24-ebs-efs-fsx-storage-selection.md) | EBS volume types (gp3 default with 3K IOPS/125 MiBps free and independent scaling, gp2 legacy with size-coupled IOPS and burst credits, io2 Block Express up to 256K IOPS/4K MiBps/sub-ms latency/99.999% durability/Multi-Attach to 16 Nitro instances, st1 throughput-optimized HDD 500 MiBps for MapReduce/Kafka, sc1 cold HDD cheapest EBS), IOPS vs throughput fundamental distinction, HDD cannot be boot volumes, EBS Snapshots (incremental block-level, Fast Snapshot Restore, Snapshot Archive 75% savings, Data Lifecycle Manager automated schedules with cross-region copy), Multi-Attach (io2-only, same-AZ, requires cluster-aware filesystem GFS2/OCFS2), Encryption (KMS transparent, cannot encrypt in-place, snapshot-copy-create migration path, default encryption per-region), RAID 0 only recommendation (RAID 5/6 waste IOPS on parity, RAID 1 wastes bandwidth), EFS performance modes (General Purpose default vs Max I/O deprecated), EFS throughput modes (Elastic auto-scaling pay-per-use default vs Provisioned fixed vs Bursting legacy trap where small filesystems throttle), EFS storage classes (Standard/IA/Archive/One Zone variants with 128 KB minimum transition threshold, automatic transition back to Standard on access), FSx for Windows File Server (SMB protocol, Active Directory integration, DFS Namespaces, shadow copies, data deduplication, Multi-AZ), FSx for Lustre (S3 data repository association with lazy loading, scratch vs persistent deployment types, up to 1000+ GBps parallel throughput for HPC/ML), FSx for NetApp ONTAP (four-protocol simultaneous NFS+SMB+iSCSI+NVMe, SSD-to-capacity-pool auto-tiering, SnapMirror 5-min RPO replication, FlexClone instant zero-copy clones, SnapLock WORM), FSx for OpenZFS (NFS with ZFS snapshots/clones/compression, up to 1M IOPS), master storage selection decision framework (block=EBS, file=EFS/FSx, object=S3), complete production architecture diagram |
| 2026-03-23 | [S3 Advanced Deep Dive](./2026/march/aws/2026-03-23-s3-advanced-deep-dive.md) | S3 storage classes (all eight classes with durability, availability, retrieval time, minimum duration, cost positioning, Express One Zone directory buckets), storage class decision framework, Intelligent-Tiering five-tier auto-optimization (Frequent, Infrequent, Archive Instant, Archive, Deep Archive with monitoring fee trade-off), lifecycle policies (transition waterfall with downward-only flow, 30/90/180-day minimum duration constraints, 128 KB minimum object size, metadata overhead trap for tiny objects, early deletion charges, AbortIncompleteMultipartUpload, NoncurrentVersionExpiration with NewerNoncurrentVersions, ExpiredObjectDeleteMarker cleanup), replication (CRR vs SRR use cases, what replicates vs what does not, no-cascade rule, Batch Replication for backfill, RTC 15-minute SLA, delete markers optional but permanent deletes never replicate), versioning (version ID behavior, delete markers, three bucket states, MFA Delete requiring root + CLI only), access points (per-application named endpoints with independent policies, VPC-restricted access points, delegation model, Multi-Region Access Points with Global Accelerator routing and failover controls), Object Lock (Governance mode with BypassGovernanceRetention override, Compliance mode truly immutable even for root, Legal Hold binary flag with no expiration, must enable at bucket creation, SEC 17a-4 compliance), event notifications (classic SNS/SQS/Lambda path vs EventBridge path with richer filtering and SQS FIFO support, at-least-once delivery, infinite loop prevention), Transfer Acceleration (CloudFront edge upload optimization, .s3-accelerate endpoint), server access logging vs CloudTrail data events comparison (best-effort free vs guaranteed paid, auth failure capture difference), S3 Select deprecation note |
| 2026-03-18 | [Global Accelerator & Edge Services](./2026/march/aws/2026-03-18-global-accelerator-edge-services.md) | Global Accelerator fundamentals (two static Anycast IPv4 addresses announced from 100+ edge locations, AWS backbone routing for 25-30% latency reduction, TCP termination at edge with proxy model), four-level hierarchy (accelerator owns Anycast IPs + DNS name, listeners on TCP/UDP/both with port ranges, endpoint groups one-per-region with traffic dials 0-100%, endpoints ALB/NLB/EC2/EIP with weights 0-255), client affinity (NONE 5-tuple hash vs SOURCE_IP 2-tuple hash), traffic dials vs endpoint weights (regional traffic control vs per-endpoint distribution, blue-green regional migration, canary within region), standard vs custom routing accelerators (standard load-balances with health/proximity/weights, custom creates deterministic port-to-instance mapping for gaming/VoIP with deny-by-default AllowCustomRoutingTraffic API, VPC subnet endpoints only, no health checks), three-way comparison (Route53 DNS layer with TTL-dependent failover vs CloudFront HTTP caching at 750+ POPs vs GA TCP/UDP acceleration with instant failover and static IPs), failover speed analysis (GA sub-30s network-level reroute vs Route53 90-300s DNS TTL-dependent), cross-region failover patterns (active-active multi-region, blue-green with traffic dials, GA+Route53+CloudFront full edge stack), two network zones per edge for infrastructure fault tolerance, Shield Standard automatic DDoS protection at edge with origin IP masking, pricing model ($0.025/hour fixed + DT-Premium $0.015-$0.101/GB dominant direction only), integration patterns with Route53 alias records and CloudFront hostname splitting |
| 2026-03-16 | [ALB vs NLB vs GLB](./2026/march/aws/2026-03-16-alb-vs-nlb-vs-glb.md) | ALB (Layer 7 HTTP/HTTPS/gRPC content-based routing, listener rules with six condition types, weighted target groups for canary deployments, WAF integration, OIDC/Cognito authentication actions), NLB (Layer 4 TCP/UDP/TLS pass-through, static Elastic IPs per AZ, source IP preservation, flow hash routing, PrivateLink support, NLB-to-ALB target pattern for static IPs + Layer 7 routing), GLB (Layer 3 transparent gateway, GENEVE encapsulation on port 6081, GWLBe as route table next-hop, third-party appliance insertion for Palo Alto/Fortinet/Check Point, GLB vs Network Firewall decision), target groups (instance/IP/Lambda/ALB types, round robin vs least outstanding requests vs weighted random algorithms, slow start mode 30-900s), health checks (asymmetric thresholds: unhealthy=2 fail fast vs healthy=5 recover slow, fail-open when all targets unhealthy, health check grace period), cross-zone load balancing (ALB enabled by default free, NLB/GLB disabled by default with inter-AZ charges), connection draining (deregistration delay default 300s, Auto Scaling lifecycle hook coordination), sticky sessions (duration-based AWSALB vs application-based AWSALBAPP cookies, externalize state instead), architectural patterns (CloudFront+ALB, NLB+ALB, GLB+TGW centralized inspection) |
| 2026-03-13 | [CloudFront Deep Dive](./2026/march/aws/2026-03-13-cloudfront-deep-dive.md) | CloudFront four-tier cache hierarchy (750+ POPs, 15 RECs, Origin Shield, origin), distributions and origin types (S3, ALB/NLB, custom HTTP, origin groups for failover), cache behaviors (path pattern routing, precedence rules, multi-origin single-domain pattern), cache policies vs origin request policies (cache key composition, forwarding separation, managed policies CachingOptimized/CachingDisabled), OAC vs legacy OAI (SigV4, SSE-KMS support, all regions, dynamic requests), VPC Origins (private ALB/NLB without public exposure, AWS backbone connectivity), Lambda@Edge vs CloudFront Functions (four trigger points, runtime/memory/network/location differences, decision framework), content security (signed URLs vs signed cookies, canned vs custom policies, key groups, geo-restriction), cache invalidation vs versioned file names (two-tier TTL strategy, cost model), logging and monitoring (standard logs, real-time logs via Kinesis, CloudWatch metrics including CacheHitRate) |
| 2026-03-12 | [Route53 Advanced -- Hybrid DNS](./2026/march/aws/2026-03-12-route53-advanced-hybrid-dns.md) | Private Hosted Zones (VPC association, cross-account sharing via authorization + acceptance, enableDnsSupport/enableDnsHostnames prerequisites), split-view DNS (same domain resolving differently internal vs external, NXDOMAIN trap where PHZ shadows public zone without fallthrough), DNSSEC (two-key model with KSK backed by asymmetric KMS key ECC_NIST_P256 in us-east-1 and ZSK fully managed by Route53, chain of trust via DS record at registrar, signing vs validation as independent features), Route53 Resolver endpoints (inbound ENIs as destinations for on-prem DNS forwarding, outbound ENIs as sources for VPC-to-on-prem forwarding, 10K QPS per ENI, static IPs for stability), forwarding rules (Forward/System/Recursive types, most-specific match wins, sharing via RAM), centralized hybrid DNS architecture (shared-services VPC with Resolver endpoints connected via Transit Gateway, single set of on-prem forwarder targets), Route 53 Profiles (bundling PHZ associations + forwarding rules + DNS Firewall rules into shareable units for 100+ account Organizations), multi-VPC DNS patterns (decentralized vs shared PHZ vs centralized hub), DNS query resolution order (forwarding rules > PHZ > public DNS) |
| 2026-03-11 | [Route53 Routing Policies & Health Checks](./2026/march/aws/2026-03-11-route53-routing-policies.md) | Route53 seven routing policies (Simple, Weighted, Latency, Failover, Geolocation, Geoproximity, Multivalue Answer), alias vs non-alias records (zone apex, free queries, Evaluate Target Health), four health check types (endpoint with string matching, calculated with AND/OR/threshold, CloudWatch alarm for private resources, ARC routing controls for data-plane failover), complex nested routing trees (failover > latency > weighted decision trees, health propagation via alias chains), active-active vs active-passive failover patterns, data plane vs control plane reliability (health checks and ARC on data plane, console/API on control plane in us-east-1), failover timing analysis (10-300s detection + TTL expiration), cost optimization with alias queries |
| 2026-03-10 | [AWS Network & Application Protection](./2026/march/aws/2026-03-10-aws-network-application-protection.md) | AWS WAF (Web ACLs, rule types, managed rule groups, WCU capacity budget, Bot Control, ATP, labels, Count mode testing, logging), AWS Shield (Standard free L3/L4 vs Advanced paid L3/L4/L7, DRT, proactive engagement, health-based detection, automatic L7 mitigation via WAF, cost protection credits, protection groups), AWS Network Firewall (firewall endpoints in dedicated subnets, stateless 5-tuple engine vs stateful Suricata IPS/IDS, domain filtering, TLS SNI inspection, strict vs action order, distributed vs centralized inspection VPC with TGW appliance mode), AWS Firewall Manager (prerequisites, policy types for WAF/Shield/NFW/SG/DNS Firewall, first/last rule group sandwich model, auto-remediation, Security Hub integration), layered defense architecture across OSI layers, decision framework |
| 2026-03-09 | [AWS KMS & Encryption Deep Dive](./2026/march/aws/2026-03-09-aws-kms-encryption-deep-dive.md) | KMS key ownership models (customer managed, AWS managed, AWS owned), key types (symmetric AES-256-GCM, asymmetric RSA/ECC, HMAC), key policies as primary access control (unique zero-default behavior), IAM integration (three authorization paths), grants (temporary delegated access, eventual consistency, grant tokens), envelope encryption (GenerateDataKey flow, data key caching), cross-account KMS access (two-policy requirement, Key ARN mandatory), key rotation (automatic 90-2560 days, on-demand, manual with aliases), multi-region keys (primary-replica model, shared key material, independent policies), KMS vs CloudHSM (multi-tenant vs single-tenant, FIPS 140-2 Level 3, custom key stores), encryption context (AAD, audit trail, policy conditions), S3 encryption options (SSE-S3/SSE-KMS/SSE-C, Bucket Keys for cost optimization), EBS/RDS/Lambda/Secrets Manager integration patterns |
| 2026-03-06 | [AWS Security Services -- Threat Detection](./2026/march/aws/2026-03-06-aws-security-services-threat-detection.md) | GuardDuty (foundational data sources, protection plans for S3/EKS/RDS/Lambda/Malware/Runtime, finding type naming convention, severity levels, Extended Threat Detection for multi-stage attacks), AWS Config (configuration recorder, configuration items, managed vs custom rules, conformance packs, detective vs proactive evaluation, auto-remediation via SSM, multi-account aggregator), Amazon Inspector (EC2/ECR/Lambda scanning, CVE database, environment-aware risk scoring, network reachability findings, continuous scanning), Security Hub (ASFF normalization, security standards CIS/FSBP/PCI DSS/NIST, cross-region aggregation, automation rules, central configuration via Organizations), full detection-to-remediation pipeline (finding to EventBridge to Lambda/SSM/SNS), Security Tooling account architecture with delegated admin for all four services, Log Archive separation of duties |
| 2026-03-05 | [IAM Advanced Patterns](./2026/march/aws/2026-03-05-iam-advanced-patterns.md) | Seven IAM policy types and how they interact, policy evaluation flowcharts (same-account vs cross-account), permission boundaries (delegation pattern, privilege escalation prevention), session policies (per-session scoping), cross-account AssumeRole (trust policies, External IDs, confused deputy), SAML 2.0 federation flow, OIDC federation (GitHub Actions OIDC, EKS IRSA connection), IAM Identity Center (permission sets, identity sources, auto-created roles), full restriction stack (SCP > boundary > identity policy > session policy) |
| 2026-03-03 | [AWS Control Tower & Landing Zones](./2026/march/aws/2026-03-03-aws-control-tower-landing-zones.md) | Control Tower as orchestration layer on Organizations, landing zone setup (Security OU, Log Archive, Audit accounts), three control behaviors (preventive/SCPs, detective/Config Rules, proactive/CloudFormation Hooks), three guidance levels (mandatory, strongly recommended, elective), Account Factory provisioning, Account Factory for Terraform (AFT) GitOps pipeline, drift detection, StackSets deployment mechanism, Control Tower vs manual Organizations comparison |
| 2026-03-02 | [AWS Organizations & SCPs](./2026/march/aws/2026-03-02-aws-organizations-scps.md) | Organization structure (management account, OUs, root), multi-account strategy (Security/Infrastructure/Workloads/Sandbox OUs), SCP mechanics (permission ceilings, not grants), SCP evaluation logic (Allow intersection, Deny inheritance), deny-list vs allow-list strategy, real-world SCP examples (deny root, restrict regions, enforce encryption, prevent leaving org), SCPs vs Permission Boundaries vs IAM Policies, cross-account access patterns, delegated administrator, consolidated billing, Terraform examples |
| 2026-02-25 | [AWS Site-to-Site VPN](./2026/february/aws/2026-02-25-aws-site-to-site-vpn.md) | IPSec tunnel fundamentals, Customer Gateway (CGW), VGW vs TGW termination, static vs dynamic (BGP) routing, VPN CloudHub multi-site, Accelerated VPN (Global Accelerator), dual-tunnel HA, redundant CGW pattern, ECMP bandwidth aggregation on TGW, DX vs VPN decision matrix, CloudWatch monitoring and troubleshooting |
| 2026-02-24 | [AWS Direct Connect](./2026/february/aws/2026-02-24-aws-direct-connect.md) | Dedicated vs Hosted connections, Private/Public/Transit VIFs, Direct Connect Gateway (multi-VPC, multi-region), LAG, BGP communities for failover, three resiliency models (dev/test, high, maximum), MACsec encryption, SiteLink, DX + TGW integration via Transit VIF + DXGW, DX vs VPN trade-offs |
| 2026-02-23 | [Transit Gateway Deep Dive](./2026/february/aws/2026-02-23-transit-gateway-deep-dive.md) | TGW architecture (ENIs, /28 subnets), 5 attachment types, route table mechanics (association vs propagation), network segmentation, centralized egress, centralized inspection with appliance mode, GWLB integration, inter-region peering, multi-account RAM sharing, cost model, key quotas |
| 2026-02-20 | [VPC Peering vs Transit Gateway vs PrivateLink](./2026/february/aws/2026-02-20-vpc-peering-vs-transit-gateway.md) | VPC Peering limitations (transitive routing, edge-to-edge, full mesh scaling), Transit Gateway architecture (route tables, associations, propagations, segmentation), PrivateLink provider-consumer model, cost comparison, decision framework |
| 2026-02-19 | [VPC Advanced: CIDR Planning & Subnet Strategy](./2026/february/aws/2026-02-19-vpc-advanced-cidr-subnet-strategy.md) | CIDR planning for 10+ VPCs, subnet tiers (public/private/isolated), route table design, Regional NAT Gateway, VPC endpoints, IPAM |
| 2026-02-17 | [ECS Advanced](./2026/february/aws/2026-02-17-ecs-advanced.md) | Service discovery (Cloud Map), Service Connect, App Mesh (EOL), ECS Anywhere, advanced capacity provider strategies |
| 2026-02-16 | [ECS Fundamentals](./2026/february/aws/2026-02-16-ecs-fundamentals.md) | Task definitions, services, capacity providers, Fargate vs EC2, deployment strategies |
| 2026-02-12 | [Auto Scaling Deep Dive](./2026/february/aws/2026-02-12-auto-scaling-deep-dive.md) | Scaling policies (target tracking, step, simple), predictive scaling, warm pools & lifecycle hooks |
| 2026-02-09 | [EC2 Networking](./2026/february/aws/2026-02-09-ec2-networking.md) | ENI, ENA, Enhanced Networking, ENA Express/SRD, EFA, MTU & Jumbo Frames |
| 2026-02-08 | [EC2 Purchasing Options](./2026/february/aws/2026-02-08-ec2-purchasing-options.md) | Spot, Reserved Instances, Savings Plans & Capacity Reservations |
| 2026-02-05 | [EC2 Instance Types, Placement & Tenancy](./2026/february/aws/2026-02-05-ec2-instance-types-placement-tenancy.md) | Instance families, placement groups & dedicated hosts |
| 2026-02-02 | [Terraform Project Structure](./2026/january/terraform/2026-02-02-terraform-project-structure.md) | Mono-repo vs multi-repo, environments, naming |
| 2026-01-30 | [Terraform Cloud/Enterprise](./2026/january/terraform/2026-01-30-terraform-cloud-enterprise.md) | Remote runs, Sentinel, private registry |
| 2026-01-29 | [Terraform Testing](./2026/january/terraform/2026-01-29-terraform-testing.md) | Validate, plan, terraform test, Terratest |
| 2026-01-28 | [Terraform Security](./2026/january/terraform/2026-01-28-terraform-security-best-practices.md) | Secrets, state security, tfsec, Checkov |
| 2026-01-27 | [Terraform Outputs & Remote State](./2026/january/terraform/2026-01-27-terraform-outputs-remote-state.md) | Output patterns, cross-stack references |
| 2026-01-23 | [Terraform Variables](./2026/january/terraform/2026-01-23-terraform-variables-validation.md) | Variable types, validation, sensitive vars |
| 2026-01-22 | [Terraform Providers](./2026/january/terraform/2026-01-22-terraform-providers.md) | Configuration, aliases, version constraints |
| 2026-01-20 | [Terraform Data Sources & Locals](./2026/january/terraform/2026-01-20-terraform-data-sources-locals.md) | Querying resources, local values |
| 2026-01-19 | [Terraform Functions](./2026/january/terraform/2026-01-19-terraform-functions-expressions.md) | Built-in functions, for expressions |

## 📂 Archive

| Year | Entries | Topics |
|------|---------|--------|
| [2026](./2026/) | 46 | Docker (Networking, Storage, Multi-Stage Builds, Runtimes), Terraform (State, Modules, Workspaces, Functions, Data Sources, Providers, Variables, Outputs, Security, Testing, Cloud/Enterprise, Project Structure), AWS (EC2, EC2 Purchasing Options, EC2 Networking, Auto Scaling, ECS, ECS Advanced, VPC Advanced, VPC Peering vs Transit Gateway, Transit Gateway Deep Dive, Direct Connect, Site-to-Site VPN, Organizations & SCPs, Control Tower & Landing Zones, IAM Advanced Patterns, Security Services -- Threat Detection, KMS & Encryption, Network & Application Protection, Route53 Routing Policies & Health Checks, Route53 Advanced -- Hybrid DNS, CloudFront Deep Dive, ALB vs NLB vs GLB, Global Accelerator & Edge Services, S3 Advanced Deep Dive, Block & File Storage -- EBS, EFS, FSx, RDS & Aurora Deep Dive, DynamoDB Deep Dive, ElastiCache & Caching Strategies, Disaster Recovery Strategies, Multi-Region Architecture Patterns, AWS Backup & Centralized Protection) |
| [2025](./2025/) | 10 | AWS (VPC, EKS, VPC CNI, Networking, IAM, Storage, Autoscaling, Security, Observability, Upgrades) |

---

## 🧠 Why I Do This

1. **Learn by teaching** - Writing forces me to truly understand
2. **Future reference** - Quick refresher when concepts get fuzzy
3. **Track progress** - See how far I've come over time

---

### 🔗 Connect

[![GitHub](https://img.shields.io/badge/GitHub-@devopsbishal-181717?style=flat&logo=github)](https://github.com/devopsbishal)
[![LinkedIn](https://img.shields.io/badge/LinkedIn-Bishal%20Rayamajhi-0A66C2?style=flat&logo=linkedin)](https://www.linkedin.com/in/bishal-rayamajhi-02523a243/)
